/// <reference types="node" />
import IteratorWritable = require('./iterator-writable');
import { Readable } from 'stream';
declare function streamToIterator<T = any>(readable: Readable, opts?: {
    objectMode?: boolean;
}): IteratorWritable<T>;
export = streamToIterator;
