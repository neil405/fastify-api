"use strict";
const IteratorWritable = require("./iterator-writable");
const stream_1 = require("stream");
const pump = require("pump");
function streamToIterator(readable, opts = { objectMode: true }) {
    readable = new stream_1.Readable({ objectMode: true }).wrap(readable);
    let writable = new IteratorWritable(opts);
    pump(readable, writable);
    return writable;
}
module.exports = streamToIterator;
//# sourceMappingURL=index.js.map